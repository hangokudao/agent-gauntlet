# default

Use the general Agent Gauntlet checks:

- identify obvious authentication, authorization, input, and data exposure risks
- prefer concrete reproduction steps over broad advice
- keep all actions inside the selected run mode
- do not test third-party systems unless explicitly authorized
